---
phase: 01-rules-extraction
plan: 03
type: execute
---

<objective>
Complete Phase 1 by identifying and extracting any computational rules from rules/docs/ not yet captured in rules/constants/ or rules/formulas/.

Purpose: Ensure all machine-readable formulas and constants are extracted before Phase 2 uses them to design the database schema and contract engine.
Output: Updated YAML formula files and/or JSON constant files with any missing computational rules.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rules-extraction/01-01-SUMMARY.md
@.planning/phases/01-rules-extraction/01-02-SUMMARY.md

# Key files (all rules/docs/ markdown files):
@rules/docs/contracts.md
@rules/docs/contract_tools.md
@rules/docs/free_agency.md
@rules/docs/salary_cap.md
@rules/docs/auctions.md
@rules/docs/rosters.md
@rules/docs/trades.md
@rules/docs/drafts.md
@rules/docs/lineups.md
@rules/docs/season.md

# Existing formula/constant files to cross-reference:
@rules/formulas/extensions.yaml
@rules/formulas/contract_tools.yaml
@rules/formulas/salary_cap.yaml
@rules/formulas/free_agency.yaml
@rules/constants/contracts.json
@rules/constants/league.json
@rules/constants/rosters.json
@rules/constants/salary_cap.json

**Constraining decisions:**
- 01-01: Lean JSON format — no _note, _formula, _rationale fields in constants
- 01-01: Used pay-in table value ($3,810) for prize pool
- 01-02: Bylaws wins over old EPV code when conflicts exist
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gap analysis — cross-reference docs against existing YAML/JSON</name>
  <files>N/A (read-only analysis)</files>
  <action>
Read all 10 rules/docs/ markdown files and identify every computational rule, formula, or numeric constant that is NOT already captured in the existing rules/formulas/*.yaml or rules/constants/*.json files.

For each doc file, extract:
- Formulas with inputs/outputs (e.g. rookie salary smoothing, UDFA budget formula)
- Numeric constants not in JSON (e.g. 120-year cap, contract year limits by type, contract default years)
- Computational rules that would need to be coded (e.g. 10% annual salary inflation, waiver claim contract preservation)

Categorize each gap as:
- FORMULA → belongs in rules/formulas/*.yaml
- CONSTANT → belongs in rules/constants/*.json
- PROCESS_RULE → not a formula or constant, just a procedural rule (skip — these are already in docs)

Produce a structured list of gaps with: source doc, section, what's missing, and target file.
  </action>
  <verify>Gap list produced with clear categorization. Every computational rule from docs has been checked against existing YAML/JSON.</verify>
  <done>Complete gap list showing all FORMULA and CONSTANT items not yet in structured files, with zero PROCESS_RULE items included</done>
</task>

<task type="auto">
  <name>Task 2: Extract missing formulas and constants into structured files</name>
  <files>rules/formulas/extensions.yaml, rules/formulas/contract_tools.yaml, rules/formulas/salary_cap.yaml, rules/formulas/free_agency.yaml, rules/constants/contracts.json, rules/constants/rosters.json, rules/constants/salary_cap.json, rules/constants/league.json</files>
  <action>
For each FORMULA gap from Task 1, add the formula to the appropriate existing YAML file following the established format (description, formula, variables, notes). Create a new YAML file only if no existing file is a natural fit.

For each CONSTANT gap from Task 1, add to the appropriate existing JSON file following the lean format established in 01-01 (raw values only, no _note fields).

Known likely gaps to check for (may find more or fewer during Task 1):
- Rookie minimum salary smoothing formula (contracts.md): "smoothed over 3 years of a UDFA contract" using NFL rookie minimums and 10% inflation → target: a new rules/formulas/contracts.yaml or add to extensions.yaml
- 10% annual salary inflation rule → target: formulas
- Contract year limits by type (R/F: 2 max, UDFA: 3 max, Drafted Rookie: 4 min/max, etc.) → target: contracts.json
- Contract default years table (RFA/B/R/UFA: 1, R/F: 2, UDFA: 3, etc.) → target: contracts.json
- 120-Year Cap (team-wide contract years limit) → target: rosters.json or contracts.json
- UDFA auction budget formula (81% of NFL Undrafted Rookie Reservation) → target: formulas
- R/F auction budget ($100k) → target: constants
- Emergency add salary (SD minimum rounded up to $100k) → target: constants or formulas
- Empty roster spot charge (veteran minimum per empty spot below 45 for cap snapshots) → target: salary_cap.yaml or salary_cap.json
- Trade cash limits (5% of ADL salary cap, $10k increments) → target: constants
- Bid error penalty (1% of ADL salary cap) → target: constants (may already be there)

Do NOT add process/procedural rules — only computational formulas and numeric values the contract engine will need.
  </action>
  <verify>
For each added item:
1. Verify it traces to a specific bylaws passage in rules/docs/
2. Verify it wasn't already present in the target file
3. Verify JSON files parse correctly: python3 -c "import json; json.load(open('rules/constants/contracts.json'))"
4. Verify YAML files parse correctly: python3 -c "import yaml; yaml.safe_load(open('rules/formulas/FILE.yaml'))" for each modified YAML file
  </verify>
  <done>All identified FORMULA and CONSTANT gaps extracted into structured files. All JSON/YAML files parse without errors. No process rules added.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 10 rules/docs/ files cross-referenced against existing YAML/JSON
- [ ] All computational formulas now in YAML files
- [ ] All numeric constants now in JSON files
- [ ] All JSON files parse: python3 -c "import json; [json.load(open(f'rules/constants/{f}')) for f in ['contracts.json','league.json','rosters.json','salary_cap.json']]"
- [ ] All YAML files parse: python3 -c "import yaml; [yaml.safe_load(open(f'rules/formulas/{f}')) for f in ['extensions.yaml','contract_tools.yaml','salary_cap.yaml','free_agency.yaml']]" (plus any new YAML files)
- [ ] No duplicate entries between YAML files
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Phase 1 (Rules Extraction) is complete — all bylaws rules are now in structured format (docs/MD, constants/JSON, formulas/YAML)
</success_criteria>

<output>
After completion, create `.planning/phases/01-rules-extraction/01-03-SUMMARY.md`

# Phase 1 Plan 03: Extract Missing Rules Summary

**[Substantive one-liner describing what was extracted]**

## Accomplishments
- [gaps found and extracted]

## Files Created/Modified
- [files changed]

## Decisions Made
[Key decisions and rationale]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Phase 1 complete, ready for Phase 2 (Foundation)
</output>
