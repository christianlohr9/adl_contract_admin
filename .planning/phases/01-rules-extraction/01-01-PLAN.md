---
phase: 01-rules-extraction
plan: 01
type: execute
---

<objective>
Verify all rule constants (JSON files) against the ADL Bylaws source of truth, ensuring every number, threshold, and structural value exactly matches.

Purpose: Accuracy is the #1 priority for Phase 1. One wrong constant breaks the contract engine in Phase 4. These files exist but have never been verified against the bylaws.
Output: Four verified, accurate JSON files in `rules/constants/` ready for engine consumption.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/1-rules-extraction/1-CONTEXT.md

# Source of truth:
@Analytics Dynasty League Bylaws 2025.md

# Files to verify:
@rules/constants/contracts.json
@rules/constants/league.json
@rules/constants/rosters.json
@rules/constants/salary_cap.json

**Constraining decisions:** None (first phase)

**Format guidance from CONTEXT.md:**
- JSON files should be raw values only — no descriptions or bylaw section references
- Group by use/contract type, not by bylaw article structure
- Every number must exactly match the bylaws
- Completeness — every rule that affects contract calculations must be captured
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify contracts.json and league.json</name>
  <files>rules/constants/contracts.json, rules/constants/league.json</files>
  <action>
Read bylaws sections I (Philosophy — for league structure), II (Governance — commissioner names), III (Rosters — for conference/division structure), IV (Season — weeks, bonus games, playoffs), VI (Contracts — salary minimums, contract year limits, growth rate, signing rules), VIII (Auctions — draft rounds, budgets, timers, bid rules), IX (Drafts — compensatory picks, draft format), XIV (Finances — fees).

Cross-reference EVERY numeric value in contracts.json and league.json against the bylaws. For each value:
1. Find the exact bylaws passage that states this value
2. Confirm the value matches exactly
3. Flag any discrepancy

Check for GAPS — values in the bylaws that should be in these files but aren't. Specifically check:
- All salary minimum tables (veteran, rookie, SD minimum) with historical values
- Contract year limits for every contract type (R/F, UDFA, drafted rookie by round, etc.)
- Auction parameters (budgets, timers, bid increments, player limits)
- UFA discount formula parameters
- All contract designations and their meanings

Fix any inaccuracies. Add any missing values. Remove any values not supported by the bylaws. Keep format lean — raw values, no bylaw section references.
  </action>
  <verify>Read both JSON files end-to-end. Every numeric value must trace to a specific bylaws passage. No value should be invented or assumed.</verify>
  <done>contracts.json and league.json are 100% accurate against bylaws with no gaps in contract-affecting values</done>
</task>

<task type="auto">
  <name>Task 2: Verify rosters.json and salary_cap.json</name>
  <files>rules/constants/rosters.json, rules/constants/salary_cap.json</files>
  <action>
Read bylaws sections III (Rosters — roster sizes, taxi squad, IR, position eligibility, cutdowns, waivers, top 43 rule, contract years cap), V (Lineups — starter positions and counts), XII (Salary Cap Penalties — cap formula, rollover, snapshots, penalty rates, auction rules, trade cash limits, special penalties).

Cross-reference EVERY numeric value in rosters.json and salary_cap.json against the bylaws. For each value:
1. Find the exact bylaws passage
2. Confirm exact match
3. Flag discrepancies

Check for GAPS — specifically:
- All roster size limits at each phase (early offseason, late offseason, regular season)
- Taxi squad rules (S/H limits, temporarily eligible limits)
- Position eligibility lineup slots with min/max ranges
- 120-year contract years cap details
- Waiver period durations
- Salary cap formula and rounding rules
- Rollover cap percentage
- All snapshot weeks and components
- All penalty rates (NG, SD year 1, SD additional years, FG with compounding)
- Trade cash limits and increments
- NEFT bid error penalty
- Suspended/retired/deceased penalty rules
- Future deposit thresholds

Fix inaccuracies, add missing values, remove unsupported values. Keep lean format.
  </action>
  <verify>Read both JSON files end-to-end. Every value must trace to the bylaws. No gaps in cap-affecting or roster-affecting constants.</verify>
  <done>rosters.json and salary_cap.json are 100% accurate against bylaws with no gaps</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 4 JSON files cross-referenced against bylaws
- [ ] Every numeric value verified or corrected
- [ ] No gaps in contract-affecting constants
- [ ] No invented or assumed values
- [ ] Files are lean (raw values, no bylaw citations)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Every value in all 4 JSON files matches the bylaws exactly
- No missing constants that affect contract calculations
</success_criteria>

<output>
After completion, create `.planning/phases/01-rules-extraction/01-01-SUMMARY.md`
</output>
