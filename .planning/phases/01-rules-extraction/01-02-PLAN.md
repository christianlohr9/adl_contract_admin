---
phase: 01-rules-extraction
plan: 02
type: execute
---

<objective>
Verify all rule formulas (YAML files) against the ADL Bylaws and cross-reference against old EPV code, ensuring every formula, eligibility rule, and process exactly matches.

Purpose: Formula accuracy is critical — these YAML files will directly drive the contract engine in Phase 4. The old EPV code provides a second verification source for extension calculations.
Output: Four verified, accurate YAML files in `rules/formulas/` ready for engine consumption.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/1-rules-extraction/1-CONTEXT.md

# Source of truth:
@Analytics Dynasty League Bylaws 2025.md

# Files to verify:
@rules/formulas/extensions.yaml
@rules/formulas/contract_tools.yaml
@rules/formulas/salary_cap.yaml
@rules/formulas/free_agency.yaml

# Cross-reference source (old EPV code):
@app/services/epv_calculations.py

**Constraining decisions:** None (first phase)

**Prior plan output:** 01-01 verified all JSON constants. Formulas may reference constant values — ensure consistency.

**Format guidance from CONTEXT.md:**
- Every formula must exactly match the bylaws
- Completeness — every rule affecting contract calculations must be captured
- Group by use/contract type
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify extensions.yaml and contract_tools.yaml</name>
  <files>rules/formulas/extensions.yaml, rules/formulas/contract_tools.yaml</files>
  <action>
Read bylaws section X (Contract Tools) completely — this is the most formula-dense section covering:
- X-A: Contract Extensions (EXT) — PR calculation, Performance Salary, EPV variants, Extended Years Salary, salary smoothing, eligibility rules
- X-B: In-Season Extensions (iEXT) — window, week coding
- X-C: Franchise Tags — EFT/NEFT/TT salary formulas, auction rules, compensation, consecutive tag limits
- X-D: ERFA Tenders and RFA Tenders — all 4 RFA tag formulas (FRFA/SRFA/ORFA/RRFA), eligibility
- X-E: Buyouts/Restructures — process, salary formulas, GM options, cap penalty calculation
- X-F: 5th Year Option — salary tiers, percentile thresholds
- X-G: Proven Performance Escalator — Level 1/2/3 thresholds and salaries

Cross-reference EVERY formula in extensions.yaml against bylaws X-A, X-B, X-F, X-G:
1. PR Starter Floor formula and rounding
2. Position Rank determination (higher of total pts rank and PPG rank)
3. Performance Salary formula — SAL(2*PR-3) and SAL(2*PR-2) averaging, PR=1 special case
4. All 3 EPV variants (curr, new, old) — definitions, when each applies
5. Extended Years Salary — MAX formula, 75% floor (82.5% for expired), per-year multiplier
6. Salary smoothing — growth rate, total value calculation, smoothed annual salary
7. Eligibility rules — all disqualifiers (contract years >= 2, rookie contract restrictions, cooldown, no robust PRs)
8. 5YO salary tiers and percentile thresholds
9. PPE level thresholds and salary references

Cross-reference extensions.yaml against OLD EPV CODE (`app/services/epv_calculations.py`):
- Verify `calculate_single_salary` uses rank*2-3 and rank*2-2 (matches Performance Salary)
- Verify EYS formula: `max_horizontal * (1.15 - 0.05 * ext_yrs)` matches Extended Years Salary
- Verify smoothing: growth_rate=1.1, denominator formula matches ext_salary_smoothing
- Verify PR selection: min of tot_pts_rank, avg_pts_rank (should be HIGHER rank = lower number)
- Note: old code uses `min_rank` — verify this matches bylaws "higher rank" definition
- Flag any discrepancy between old code and bylaws (bylaws win)

Cross-reference EVERY formula in contract_tools.yaml against bylaws X-C, X-D, X-E:
1. Franchise tag salary formulas (EFT initial, July 1 update, NEFT, TT) — Top 5 vs Top 10, ADL Cap %, 120% prev salary
2. NEFT/TT auction opening bid formulas
3. Consecutive tag limit formula (1.44x, 1.20x, highest positional)
4. All RFA tender formulas (FRFA 2.20x, SRFA 1.65x, ORFA 1.10x, RRFA at NFL price)
5. ERFA tender formula
6. B/R salary formula and GM options
7. All eligibility rules and restrictions
8. Match windows, compensation requirements, contract year rules

Fix any inaccuracies. Add any missing formulas or rules. Flag any bylaws vs old code conflicts.
  </action>
  <verify>Read both YAML files. Every formula must trace to a specific bylaws passage. Cross-check critical EPV formulas against old code. No gaps in extension or contract tool calculations.</verify>
  <done>extensions.yaml and contract_tools.yaml are 100% accurate against bylaws, with old EPV code discrepancies documented</done>
</task>

<task type="auto">
  <name>Task 2: Verify salary_cap.yaml and free_agency.yaml</name>
  <files>rules/formulas/salary_cap.yaml, rules/formulas/free_agency.yaml</files>
  <action>
Read bylaws sections VII (Free Agency) and XII (Salary Cap Penalties) completely:

**Section XII — Salary Cap Penalties:**
- ADL salary cap formula (0.81 * NFL cap, rounding rule)
- Franchise salary cap = ADL cap + rollover
- Rollover formula and cap
- Salary expenditures (snapshot averaging, snapshot components including empty roster spots)
- NG penalty (0%)
- SD penalty (60% year 1 + 30% per additional year) — verify exact formula wording
- FG penalty (100% with 10% compounding) — verify multi-year split (ceil/floor 50%)
- Cap penalty rounding ($10k)
- Cap penalty acceleration (pre-July 1 vs post-July 1 behavior)
- July 1 Tenders (quantity, usage rules, waiver claim return)
- B/R cap penalty formula (virtual contract approach)
- Suspended player penalty (50%)
- Retired player penalty (0%, Barry Sanders Rule)
- Deceased player penalty (0%)

**Section VII — Free Agency:**
- RFA vs UFA eligibility criteria
- Waiver system rules and timing
- UFA Auction — starting bid, bid increment, clock, proxy bids
- UFA salary formula with multi-year discount
- Post-Final Roster Cutdown UFA rules
- Player drop protection (2-hour window)
- Add/Drop provisions

Cross-reference EVERY formula in salary_cap.yaml:
1. Cap formula and rounding
2. Rollover formula and 10% cap
3. Salary expenditures calculation with all 4 snapshot weeks
4. All 3 penalty types (NG, SD, FG) with exact formulas
5. FG compounding and multi-year split
6. Acceleration rules
7. July 1 tenders
8. B/R penalty
9. Special cases (suspended, retired, deceased)

Cross-reference EVERY formula in free_agency.yaml:
1. ERFA tender formula
2. All 4 RFA tender formulas (should match contract_tools.yaml — verify consistency)
3. RFA eligibility restrictions
4. UFA auction salary formula
5. Franchise tag formulas (EFT, NEFT, TT — should match contract_tools.yaml)
6. CFA salary cutoff and pick round thresholds
7. B/R salary formula (should match contract_tools.yaml)

Check for CROSS-FILE CONSISTENCY:
- Formulas that appear in both contract_tools.yaml and free_agency.yaml must be identical
- Formula variables must reference constants that exist in the JSON files
- No contradictions between YAML files

Fix inaccuracies, add missing formulas, resolve inconsistencies.
  </action>
  <verify>Read both YAML files. Every formula traces to bylaws. Cross-file consistency confirmed — no contradictions between YAML files or with JSON constants.</verify>
  <done>salary_cap.yaml and free_agency.yaml are 100% accurate against bylaws, cross-file consistency verified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 4 YAML files cross-referenced against bylaws
- [ ] Every formula verified or corrected
- [ ] Old EPV code cross-referenced for extension formulas
- [ ] No gaps in contract-affecting formulas
- [ ] Cross-file consistency verified (no contradictions between YAML files)
- [ ] Cross-format consistency verified (YAML formulas reference valid JSON constants)
- [ ] Phase 1 complete — all rules extracted and verified
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Every formula in all 4 YAML files matches the bylaws exactly
- Old EPV code discrepancies documented (bylaws win)
- No missing formulas that affect contract calculations
- Phase 1: Rules Extraction complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-rules-extraction/01-02-SUMMARY.md`
</output>
