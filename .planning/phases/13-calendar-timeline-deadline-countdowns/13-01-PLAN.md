---
phase: 13-calendar-timeline-deadline-countdowns
plan: 01
type: execute
---

<objective>
Fix ISS-002: Franchise tag, ERFA tender, and RFA tender eligibility checks query the wrong season for expired contracts, causing zero players to appear as eligible.

Purpose: The MFL sync stores all contracts in the current season (e.g., 2026), but eligibility checkers query `season - 1` (e.g., 2025) which returns nothing. Fix to query current season with `years_remaining == 0`.
Output: Tags and tenders return eligible players correctly; dashboard columns populate with values.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ISSUES.md

# ISS-002 details from ISSUES.md:
# check_tag_eligibility (franchise_tags.py:185), check_erfa_eligibility, and check_rfa_eligibility
# all look for a contract in `season - 1` to find expired contracts. However, MFL sync stores
# all contracts in the *current* season (2026). Fix: query `Contract.season == season` with
# `Contract.years_remaining == 0` instead of `Contract.season == season - 1`.

# Prior phase context:
@.planning/phases/12-contract-management-dashboard/12-02-SUMMARY.md

# Source files to fix:
@src/app/services/franchise_tags.py
@src/app/services/tenders.py

**Constraining decisions:**
- Phase 03-02: Player sync fetches all existing into memory lookup
- Phase 03-03: ContractType set to NG placeholder — Phase 4 classifies properly
- Phase 05-02: team_id resolved from player's contract record

**Issue being addressed:** ISS-002 (High impact — tags/tenders completely broken)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix franchise tag eligibility season queries</name>
  <files>src/app/services/franchise_tags.py</files>
  <action>
Fix three `season - 1` queries in franchise_tags.py that look up expired contracts:

1. **check_tag_eligibility** (~line 189): Change `Contract.season == season - 1` to `Contract.season == season` in the query that finds expired contracts. The subsequent `years_remaining > 0` check already validates the contract is expired.

2. **calculate_franchise_tag** (~line 303): Same fix — change `Contract.season == season - 1` to `Contract.season == season` in the query loading previous contract for tag pricing.

3. **check_transition_tag_eligibility** (if exists, similar pattern): Same fix.

**Do NOT change** `_top_n_salaries` (~line 82) — that function fetches positional salary rankings which may legitimately use the previous season's data for tag pricing. Only fix the eligibility/contract-lookup queries.

**Why this works:** MFL sync stores all contracts in the current season. Expired contracts have `years_remaining == 0` in the current season row, not in a previous season row.
  </action>
  <verify>
Run: `cd /Users/clohr/git/adl_contract_admin && python -c "
import asyncio
from src.app.database import async_session_factory
from src.app.services.franchise_tags import check_tag_eligibility
async def test():
    async with async_session_factory() as session:
        eligible, reason = await check_tag_eligibility(session, 1893, 2026)
        print(f'Bolton eligible: {eligible}, reason: {reason}')
asyncio.run(test())
"`
Should return `eligible: True` (Bolton has years_remaining=0, season=2026).
  </verify>
  <done>check_tag_eligibility returns True for players with expired contracts (years_remaining=0) in current season</done>
</task>

<task type="auto">
  <name>Task 2: Fix tender eligibility season queries</name>
  <files>src/app/services/tenders.py</files>
  <action>
Fix three `season - 1` queries in tenders.py:

1. **check_erfa_eligibility** (~line 167): Change `Contract.season == season - 1` to `Contract.season == season`.

2. **check_rfa_eligibility** (~line 234): Change `Contract.season == season - 1` to `Contract.season == season`. Also fix the contract age calculation at ~line 251: `contract_age = (season - 1) - signed_season + 1` should become `contract_age = season - signed_season` (since contract is in current season, not previous). Similarly fix `original_length` calculation at ~line 269.

3. **calculate_tender** (~line 311): Change `Contract.season == season - 1` to `Contract.season == season` in the query loading previous contract for tender pricing.

**Important:** The `eligible_seasons` check in check_erfa_eligibility (~line 190) uses `season - 1` and `season - 2` for `signed_season` validation. This is about WHEN the contract was SIGNED, not what season it's stored in. Review whether this logic is still correct: if a contract is stored in season 2026 with signed_season=2025, the check `signed_season in [2025, 2024]` should work. Keep the signed_season checks as-is unless they reference Contract.season.
  </action>
  <verify>
Run: `cd /Users/clohr/git/adl_contract_admin && python -c "
import asyncio
from src.app.database import async_session_factory
from src.app.services.tenders import check_erfa_eligibility, check_rfa_eligibility
async def test():
    async with async_session_factory() as session:
        erfa_ok, erfa_r = await check_erfa_eligibility(session, 1893, 2026)
        rfa_ok, rfa_r = await check_rfa_eligibility(session, 1893, 2026)
        print(f'ERFA: {erfa_ok} ({erfa_r})')
        print(f'RFA: {rfa_ok} ({rfa_r})')
asyncio.run(test())
"`
At least one eligibility check should return True for expired-contract players.
  </verify>
  <done>ERFA and RFA eligibility checks find expired contracts in current season; dashboard tag/tender columns populate with values</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `franchise_tags.py` eligibility queries use `Contract.season == season` (not `season - 1`)
- [ ] `tenders.py` eligibility queries use `Contract.season == season` (not `season - 1`)
- [ ] Contract age calculations in tenders.py adjusted for current-season storage
- [ ] Backend starts without errors: `cd /Users/clohr/git/adl_contract_admin && python -c "from src.app.services.franchise_tags import check_tag_eligibility; print('OK')"`
- [ ] At least one test player returns eligible for tags/tenders
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- ISS-002 resolved — franchise tag, ERFA, and RFA eligibility checks return results for expired-contract players
- No regressions in other eligibility checks (extensions, buyouts, 5YO, PPE are unaffected)
</success_criteria>

<output>
After completion, create `.planning/phases/13-calendar-timeline-deadline-countdowns/13-01-SUMMARY.md`
</output>
