---
phase: 19-extensions
plan: 01
type: execute
---

<objective>
Validate and fix EXT eligibility for all 1,549 players against the TagElig26 spreadsheet tab.

Purpose: Ensure check_extension_eligibility correctly classifies every player as oEXT, iEXT, or ineligible — matching the spreadsheet exactly.
Output: Fixed eligibility logic with zero discrepancies against TagElig26.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context (dependency graph):
@.planning/phases/18-franchise-tags/18-01-SUMMARY.md
@.planning/phases/18-franchise-tags/18-02-SUMMARY.md

# Key source files:
@src/app/services/extensions.py
@src/app/services/eligibility.py
@src/app/services/epv.py
@src/app/services/window_status.py
@rules/formulas/extensions.yaml
@rules/constants/contracts.json

# Spreadsheet reference:
# "2026 ADL Contract Admin.xlsx" — TagElig26 tab
# Columns: Franchise, Player, Age, Salary, Years, Contract, EXT*, FT, RFA, ERFA, ORFArd, 5YO, Conf, Accr Szns
# EXT* column values: "oEXT" (887 players), "iEXT" (121 players), or None (541 ineligible)
# Player name format: "Lastname, Firstname TEAM POS"

**Established patterns:**
- team_id optional param for conference-scoped queries (from 18-01)
- Phase 18 user preference: verify in production app, not via scripts

**Constraining decisions:**
- [Phase 18-01]: team_id scoping pattern for eligibility checks
- [Phase 18-02]: ADL Cap Percentage discovered — may or may not apply to extensions

**Key context:**
- oEXT = offseason extension eligible (years_remaining 0 or 1, non-rookie or post-kickoff rookie)
- iEXT = in-season extension eligible (rookie/UDFA in final year, pre-kickoff — eligible only after kickoff)
- The distinction is date-based: iEXT players become oEXT-equivalent after NFL kickoff
- TagElig26 reflects a point-in-time snapshot (likely pre-kickoff, since iEXT exists)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit EXT eligibility against spreadsheet</name>
  <files>src/app/services/extensions.py</files>
  <action>
Write a temporary inline audit (run via `uv run python -c "..."` or a short throwaway snippet) that:
1. Reads TagElig26 tab from "2026 ADL Contract Admin.xlsx" — extract player name, EXT* column value (oEXT/iEXT/None) for all 1,549 players
2. For each player, match to DB by name, call check_extension_eligibility(session, player_id, season=2026)
3. Compare: spreadsheet says oEXT/iEXT → app should say eligible=True (or eligible with iEXT distinction). Spreadsheet says None → app should say eligible=False.
4. Output a summary of discrepancies: player name, spreadsheet value, app result, app reason.

Do NOT create a permanent script file. Run it inline and capture the output. The goal is to identify what's broken, not to ship a validation tool.

Note: The app's check_extension_eligibility doesn't currently distinguish oEXT vs iEXT — it returns (True/False, reason). The iEXT distinction comes from the rookie/UDFA kickoff check. For this audit, treat both oEXT and iEXT as "eligible" from the app's perspective (the kickoff gate may cause iEXT players to show as ineligible if date.today() < kickoff — that's expected behavior, not a bug).
  </action>
  <verify>Audit output shows discrepancy count and details</verify>
  <done>Clear list of eligibility discrepancies between app and spreadsheet identified</done>
</task>

<task type="auto">
  <name>Task 2: Fix EXT eligibility discrepancies</name>
  <files>src/app/services/extensions.py, src/app/services/eligibility.py</files>
  <action>
Fix all discrepancies found in Task 1. Common expected issues:
- Rookie/UDFA contract detection may be too strict or too loose
- years_remaining boundary conditions (0 vs 1 vs 2)
- EXT-in-prior-window check may false-positive on old EXT designations
- Robust PR check may exclude players who should be eligible
- Missing team_id scoping (established in Phase 18-01)

For each fix: modify the service function directly, re-run the audit to confirm fix, repeat until 0 discrepancies (excluding expected iEXT/kickoff-gated players).

Do NOT add team_id to check_extension_eligibility unless the audit reveals conference-scoping bugs (franchise tags needed it because of dual-conference contracts — extensions may not have the same issue).
  </action>
  <verify>Re-run audit shows 0 discrepancies (excluding expected iEXT kickoff-gated differences)</verify>
  <done>All EXT eligibility matches TagElig26 spreadsheet — oEXT players show eligible, ineligible players show ineligible with correct reason</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Re-run eligibility audit: 0 unexpected discrepancies
- [ ] App server starts without errors: `uv run uvicorn app.main:app --host 0.0.0.0 --port 8000` from src/
- [ ] Spot-check 2-3 players via API: `curl localhost:8000/api/tools/extensions/{player_id}?season=2026`
</verification>

<success_criteria>
- All eligibility fixes committed
- 887 oEXT + 121 iEXT players correctly identified as eligible (iEXT may show kickoff-gated, which is correct)
- 541 ineligible players correctly rejected with appropriate reasons
- No regressions in other eligibility checks
</success_criteria>

<output>
After completion, create `.planning/phases/19-extensions/19-01-SUMMARY.md`
</output>
