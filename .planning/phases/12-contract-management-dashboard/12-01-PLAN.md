---
phase: 12-contract-management-dashboard
plan: 01
type: execute
---

<objective>
Set up the Contract Management Dashboard page with API integration, routing, and the window status bar component.

Purpose: Establish the page shell with team selector, navigation entry, and the primary value-add — a status bar showing which contract action windows are currently open/closed so GMs know at a glance what's in play.
Output: Working page at /contracts route with team selector and window status bar showing live data from the roster eligibility API.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-contract-management-dashboard/12-CONTEXT.md

# Prior phase summaries (dependency graph):
@.planning/phases/08-frontend-ui/08-03-SUMMARY.md
@.planning/phases/08-frontend-ui/08-04-SUMMARY.md
@.planning/phases/11-roster-wide-eligibility-api/11-01-SUMMARY.md
@.planning/phases/10-period-detection-eligibility/10-02-SUMMARY.md

# Key source files:
@frontend/src/api/types.ts
@frontend/src/api/client.ts
@frontend/src/api/queries/teams.ts
@frontend/src/api/queries/cap.ts
@frontend/src/App.tsx
@frontend/src/components/layout/AppSidebar.tsx
@frontend/src/pages/DashboardPage.tsx
@frontend/src/pages/RosterPage.tsx
@frontend/src/lib/format.ts

# Backend API reference:
@src/app/schemas/roster_eligibility.py
@src/app/schemas/tools.py

**Established patterns:**
- Team selector with nuqs for URL state persistence (`useQueryState("team", parseAsInteger)`)
- React Query hooks with 5-min stale time in api/queries/ files
- API client with generic get<T> in api/client.ts
- Types defined in api/types.ts matching backend Pydantic schemas
- shadcn/ui Card, Badge components for dashboard layouts
- Skeleton loading states during data fetches
- Sidebar navigation entries in AppSidebar.tsx with lucide-react icons

**Constraining decisions:**
- Phase 08-02: Salary displayed in millions matching MFL platform ($40.93)
- Phase 08-02: nuqs with react-router/v7 adapter for URL state
- Phase 11-01: Roster eligibility endpoint at GET /api/teams/{team_id}/eligibility
- Phase 11-01: Window statuses included in response as dict keyed by action name
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add API types, query hook, and route setup</name>
  <files>frontend/src/api/types.ts, frontend/src/api/queries/eligibility.ts, frontend/src/pages/ContractManagementPage.tsx, frontend/src/App.tsx, frontend/src/components/layout/AppSidebar.tsx</files>
  <action>
1. **Types** (api/types.ts): Add types matching the backend roster eligibility schemas:
   - `WindowStatusSchema` with fields: action (string), status ("open" | "closed" | "unconfigured"), opens (string | null), closes (string | null), reason (string | null)
   - `PlayerActionSummarySchema` with fields: player_id (number), player_name (string), position (string), current_salary (number | null), headline_value (number | null), headline_label (string)
   - `ActionGroupSchema` with fields: action (string), window_closes (string | null), players (PlayerActionSummarySchema[])
   - `RosterEligibilitySchema` with fields: team_id (number), season (number), action_groups (ActionGroupSchema[]), window_statuses (Record<string, WindowStatusSchema>)

2. **Query hook** (api/queries/eligibility.ts): Create query hook following existing patterns (see teams.ts, cap.ts):
   - `useRosterEligibility(teamId: number | null)` — calls `GET /api/teams/${teamId}/eligibility`, enabled only when teamId is truthy
   - Use same 5-min stale time as other queries
   - Return useQuery result

3. **Page stub** (pages/ContractManagementPage.tsx): Create minimal page component with team selector following RosterPage pattern:
   - Team selector using nuqs `useQueryState("team", parseAsInteger)` and `useTeams()` query
   - Skeleton loading state
   - Placeholder content div for status bar (Task 2 will build this)

4. **Route** (App.tsx): Add route `/contracts` pointing to ContractManagementPage, placed after `/cap` route

5. **Sidebar** (AppSidebar.tsx): Add navigation entry for "Contracts" with `FileText` icon from lucide-react, placed after Salary Cap entry, linking to `/contracts`
  </action>
  <verify>
- `cd frontend && npx tsc --noEmit` passes with no type errors
- Page renders at http://localhost:5173/contracts with team selector
- Sidebar shows "Contracts" navigation entry
  </verify>
  <done>Types match backend schemas, query hook fetches eligibility data, page accessible at /contracts route with team selector, sidebar entry visible</done>
</task>

<task type="auto">
  <name>Task 2: Build window status bar component</name>
  <files>frontend/src/components/contracts/WindowStatusBar.tsx, frontend/src/pages/ContractManagementPage.tsx</files>
  <action>
Create WindowStatusBar component that displays the 7 contract action window statuses as a horizontal strip of badges/cards at the top of the page.

1. **Component** (components/contracts/WindowStatusBar.tsx):
   - Props: `windowStatuses: Record<string, WindowStatusSchema>`
   - Render a horizontal flex row (flex-wrap for mobile) of status items
   - Each item shows: action label (human-readable), status badge, closing date if open
   - Action label mapping: extension → "Extensions", franchise_tag → "Franchise Tags", erfa_tender → "ERFA Tenders", rfa_tender → "RFA Tenders", buyout_restructure → "Buyout/Restructure", fifth_year_option → "5th Year Option", proven_performance_escalator → "PPE"
   - Status badge colors: "open" → green (bg-green-100 text-green-800 dark variants), "closed" → red/muted, "unconfigured" → gray/muted
   - If status is "open" and closes date exists, show "Closes: {formatted date}" below the badge
   - If status is "closed", show "Closed" badge, muted styling
   - If status is "unconfigured", show "Not Set" badge, muted styling
   - Use shadcn Badge component for status indicators
   - Wrap in a Card component for visual grouping, with CardHeader title "Action Windows"

2. **Integrate into page** (ContractManagementPage.tsx):
   - After team selector, render WindowStatusBar when eligibility data is loaded
   - Pass `data.window_statuses` to the component
   - Show Skeleton placeholder while loading (match height of status bar)

Format dates using simple locale formatting (new Date(dateStr).toLocaleDateString()).
  </action>
  <verify>
- `cd frontend && npx tsc --noEmit` passes
- Window status bar renders with colored badges when team is selected
- Open windows show green badges with closing dates, closed show red/muted, unconfigured show gray
  </verify>
  <done>WindowStatusBar component renders all 7 action windows with color-coded status badges and deadline dates, integrated into ContractManagementPage below team selector</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Contract Management page shell with team selector, sidebar navigation, and window status bar showing live action window data</what-built>
  <how-to-verify>
    1. Run: `cd frontend && npm run dev` (backend should be running on port 8000)
    2. Visit: http://localhost:5173/contracts
    3. Verify: Sidebar shows "Contracts" entry with icon between Salary Cap and Calendar
    4. Select a team from the team selector
    5. Verify: Window status bar appears showing action windows (Extensions, Franchise Tags, ERFA Tenders, RFA Tenders, Buyout/Restructure, 5th Year Option, PPE)
    6. Verify: Each window shows colored badge — green for open, red/muted for closed, gray for unconfigured
    7. Verify: Open windows with deadlines show closing date
    8. Verify: Loading skeleton appears while data fetches
  </how-to-verify>
  <resume-signal>Type "approved" to continue to Plan 12-02, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npx tsc --noEmit` passes
- [ ] Page accessible at /contracts route
- [ ] Sidebar entry visible and navigable
- [ ] Team selector works with URL state persistence
- [ ] Window status bar shows live data from eligibility API
- [ ] Loading states work correctly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- ContractManagementPage renders with team selector and window status bar
- API integration fetches roster eligibility data correctly
- Window statuses display with appropriate color coding
</success_criteria>

<output>
After completion, create `.planning/phases/12-contract-management-dashboard/12-01-SUMMARY.md`
</output>
