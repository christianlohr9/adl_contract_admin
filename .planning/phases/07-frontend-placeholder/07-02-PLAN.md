---
phase: 07-frontend-placeholder
plan: 02
type: execute
---

<objective>
Create placeholder pages for all views — Dashboard, Roster, Player Detail (with tabbed contract tools), and Salary Cap — so Phase 8 can fill in real components without reworking page structure.

Purpose: Establish every page shell with the right route, position in navigation, and clear indication of what will go there.
Output: All placeholder pages with meaningful structure and descriptions, ready for Phase 8 to replace with real UI.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-frontend-placeholder/07-RESEARCH.md
@.planning/phases/07-frontend-placeholder/07-CONTEXT.md
@.planning/phases/07-frontend-placeholder/07-01-SUMMARY.md

# API endpoints informing page content:
# Dashboard: GET /api/teams/{team_id}/snapshot (roster + cap + allotments)
# Roster: GET /api/teams/{team_id}/roster
# Player Detail: GET /api/players/{player_id}, GET /api/tools/{player_id}/all
# Salary Cap: GET /api/cap/teams/{team_id}, GET /api/cap/teams/{team_id}/allotments

# Contract tools tabs (from bylaws):
# Extensions (X-A/B), Tags (X-C), Tenders (X-D), Buyout (X-E), 5YO, PPE

**Established patterns:** shadcn/ui Sidebar layout from 07-01, React Router nested routes, shadcn/ui Tabs component
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Dashboard, Roster, and Salary Cap placeholder pages</name>
  <files>frontend/src/pages/DashboardPage.tsx, frontend/src/pages/RosterPage.tsx, frontend/src/pages/SalaryCapPage.tsx</files>
  <action>
Replace the minimal stubs from Plan 07-01 with structured placeholder content. Use shadcn/ui Card components for layout. No API calls — static placeholder text only.

1. **DashboardPage.tsx** — Action-oriented home page per CONTEXT.md:
   - Page heading: "Dashboard"
   - Placeholder cards (using shadcn/ui Card) for:
     - "Available Actions" — will show extensions, tags, tenders available to the team
     - "Cap Summary" — will show salary cap snapshot (total cap, used, remaining)
     - "Roster Overview" — will show roster count, pending transactions
   - Each card: CardHeader with title, CardContent with placeholder text describing what Phase 8 will build

2. **RosterPage.tsx** — Team roster view:
   - Page heading: "Roster"
   - Placeholder describing a data table of players with columns: Name, Position, Salary, Contract Type, Years Remaining
   - Placeholder text: "Click a player to view contract tools"
   - Note: rows will link to /roster/:playerId (Player Detail)

3. **SalaryCapPage.tsx** — Cap overview:
   - Page heading: "Salary Cap"
   - Placeholder cards for:
     - "Cap Summary" — total cap, total used, dead money, remaining
     - "Player Penalties" — per-player cap hit breakdown
     - "Allotments" — team allotment usage (extensions, tags, tenders remaining)

Each page should have enough structure that Phase 8 knows exactly what components to build and where they go.
  </action>
  <verify>cd frontend && npm run build — exits 0; all three pages render with Card placeholders visible in browser</verify>
  <done>Dashboard, Roster, and Salary Cap pages show structured placeholder content with Cards indicating Phase 8 component locations</done>
</task>

<task type="auto">
  <name>Task 2: Build Player Detail page with tabbed contract tools</name>
  <files>frontend/src/pages/PlayerDetailPage.tsx</files>
  <action>
Replace the minimal PlayerDetailPage stub with the key interaction pattern: player info header + tabbed contract tools view.

1. **Player header section:**
   - Display placeholder player info: Name, Position, Team, Current Salary, Contract Type, Years Remaining
   - Use static placeholder values (e.g., "Player #123")
   - Read :playerId from useParams() and display it

2. **Tabbed contract tools** using shadcn/ui Tabs:
   - Tab triggers: Extensions, Tags, Tenders, Buyout, 5YO, PPE
   - Each TabsContent: placeholder Card describing what the tool shows
     - **Extensions tab:** "Extension options (1-year through max), salary projections, eligibility status"
     - **Tags tab:** "Franchise tag (EFT, NEFT) and transition tag prices, consecutive tag premiums"
     - **Tenders tab:** "ERFA and RFA tender options (FRFA, SRFA, ORFA, RRFA) with salary calculations"
     - **Buyout tab:** "Buyout/restructure salary with GM option builder"
     - **5YO tab:** "5th Year Option pricing via NEFT/TT/modified-TT tiers"
     - **PPE tab:** "Proven Performance Escalator salary via starter percentile"
   - Default tab: Extensions

This is the most important page — it's the primary interaction pattern GMs will use (roster → click player → see contract tools).
  </action>
  <verify>cd frontend && npm run build — exits 0; navigating to /roster/123 shows player header + 6 tabs, clicking tabs switches content</verify>
  <done>Player Detail page shows placeholder header + 6 contract tool tabs, each with descriptive placeholder content, tab switching works</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete frontend scaffold with sidebar navigation and all placeholder pages</what-built>
  <how-to-verify>
    1. Run: cd frontend && npm run dev
    2. Visit: http://localhost:5173
    3. Verify: Sidebar shows "ADL Contract Admin" brand + Dashboard, Roster, Salary Cap nav items
    4. Click Dashboard: See action-oriented placeholder cards (Available Actions, Cap Summary, Roster Overview)
    5. Click Roster: See placeholder for player data table with click-to-detail instruction
    6. Click Salary Cap: See cap summary, penalties, allotments placeholder cards
    7. Navigate to http://localhost:5173/roster/123: See player header + 6 contract tool tabs
    8. Click through all 6 tabs: Extensions, Tags, Tenders, Buyout, 5YO, PPE — each shows placeholder description
    9. Verify sidebar active state highlights current page
    10. Resize browser narrow: Sidebar collapses to icon mode
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `cd frontend && npm run build` succeeds with no errors
- [ ] All 4 pages render with structured placeholder content
- [ ] Player Detail has 6 working tabs
- [ ] Navigation between all pages works via sidebar
- [ ] No console errors in browser
- [ ] Phase 7 complete — frontend scaffold ready for Phase 8
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Human verified navigation flow works
- Every placeholder page clearly indicates what Phase 8 will build there
- Phase 7 complete
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-placeholder/07-02-SUMMARY.md`
</output>
