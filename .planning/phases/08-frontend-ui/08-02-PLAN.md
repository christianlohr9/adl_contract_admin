---
phase: 08-frontend-ui
plan: 02
type: execute
---

<objective>
Build the roster browsing and player search experience — roster page with sortable data table, team selector, and global player search command palette.

Purpose: Enable GMs to browse rosters and quickly find any player across the league.
Output: Functional roster page with DataTable and global search in the sidebar header.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-frontend-ui/08-RESEARCH.md
@.planning/phases/08-frontend-ui/08-CONTEXT.md
@.planning/phases/08-frontend-ui/08-01-SUMMARY.md

# Key source files:
@frontend/src/pages/RosterPage.tsx
@frontend/src/components/layout/AppSidebar.tsx
@frontend/src/components/layout/AppLayout.tsx
@frontend/src/components/data-table/DataTable.tsx
@frontend/src/api/queries/teams.ts
@frontend/src/api/queries/players.ts
@frontend/src/api/types.ts
@frontend/src/lib/format.ts

**Tech stack available:** TanStack Query, TanStack Table, shadcn/ui (table, command, dialog, select, badge), nuqs
**Established patterns:** DataTable generic component, query hooks with staleTime 5min, formatSalary utility
**Constraining decisions:**
- Roster data from GET /api/teams/{teamId}/roster (RosterEntrySchema)
- Player search from GET /api/players/search?q={query}
- Click row → navigate to /roster/{playerId}
- Desktop-first, clean minimal table design
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build roster page with DataTable and team selector</name>
  <files>frontend/src/pages/RosterPage.tsx, frontend/src/components/roster/columns.tsx</files>
  <action>
1. Create `frontend/src/components/roster/columns.tsx` — column definitions for roster table:
   - Player Name (sortable, link to /roster/{playerId})
   - Position (sortable, use Badge component for visual distinction)
   - Salary (sortable, formatted with formatSalary)
   - Contract Type (sortable, formatted with formatContractType)
   - Years Remaining (sortable)
   - Roster Status (sortable)
   - Use DataTableColumnHeader for sortable columns

2. Rewrite `frontend/src/pages/RosterPage.tsx`:
   - Add team selector dropdown at top using shadcn Select, populated from useTeams() hook
   - Default to first team (or use nuqs to persist selected team in URL: ?team={teamId})
   - Fetch roster with useTeamRoster(selectedTeamId)
   - Render DataTable with roster columns
   - Handle loading state (show skeleton/spinner)
   - Handle empty state ("No players on roster")
   - Row click navigates to /roster/{playerId} using react-router navigate
   - Position filter using shadcn Select (All, QB, RB, WR, TE, etc.)
  </action>
  <verify>cd frontend && pnpm build succeeds; dev server shows roster page with team selector and sortable table</verify>
  <done>Roster page renders team roster in sortable DataTable with team selector, position filter, salary formatting, and row click navigation</done>
</task>

<task type="auto">
  <name>Task 2: Add player search command palette to sidebar</name>
  <files>frontend/src/components/player/PlayerSearch.tsx, frontend/src/components/layout/AppSidebar.tsx</files>
  <action>
1. Create `frontend/src/components/player/PlayerSearch.tsx`:
   - Search trigger button in the sidebar that opens CommandDialog on click
   - Also bind Cmd+K (Mac) / Ctrl+K keyboard shortcut to open
   - Use shadcn CommandDialog with CommandInput, CommandList, CommandItem, CommandEmpty
   - On input change, call usePlayerSearch(debounced query) — only fetch when query >= 2 chars
   - Show player name, position, and NFL team in each CommandItem
   - On select, navigate to /roster/{playerId} and close dialog
   - Show loading state while fetching

2. Add PlayerSearch to `frontend/src/components/layout/AppSidebar.tsx`:
   - Place search trigger button in the sidebar header area (above navigation links)
   - Style as a subtle search bar: "Search players..." placeholder text with search icon
  </action>
  <verify>cd frontend && pnpm build succeeds; Cmd+K opens search, typing finds players, selecting navigates to detail</verify>
  <done>Player search command palette accessible from sidebar and Cmd+K shortcut; searches API, navigates on selection</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Roster page with sortable data table and global player search</what-built>
  <how-to-verify>
    1. Run: cd frontend && pnpm dev
    2. Make sure backend is running (docker compose up)
    3. Visit: http://localhost:5173/roster
    4. Test: Select a team from dropdown — roster table populates
    5. Test: Click column headers to sort (name, salary, position)
    6. Test: Filter by position
    7. Test: Click a player row — navigates to /roster/{playerId}
    8. Test: Press Cmd+K — search dialog opens
    9. Test: Type a player name — results appear
    10. Test: Click a search result — navigates to player detail
    11. Confirm: Salaries display as formatted USD (e.g., "$750,000" not "0.75")
    12. Confirm: Table is clean with whitespace, not cluttered
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && pnpm build` succeeds
- [ ] Roster page shows sortable data table with team roster
- [ ] Team selector switches between teams
- [ ] Player search finds players across all teams
- [ ] Navigation from roster/search to player detail works
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- GMs can browse any team's roster and search for any player
  </success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-ui/08-02-SUMMARY.md`
</output>
