---
phase: 08-frontend-ui
plan: 04
type: execute
---

<objective>
Build the salary cap page with charts and penalty table, and the action-driven dashboard that surfaces what GMs need to act on.

Purpose: Give GMs a clear cap picture with visualizations and an action-item homepage that drives decision-making.
Output: Functional salary cap page with bar chart and penalty breakdown, and dashboard with actionable widgets.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-frontend-ui/08-RESEARCH.md
@.planning/phases/08-frontend-ui/08-CONTEXT.md
@.planning/phases/08-frontend-ui/08-01-SUMMARY.md
@.planning/phases/08-frontend-ui/08-02-SUMMARY.md
@.planning/phases/08-frontend-ui/08-03-SUMMARY.md

# Key source files:
@frontend/src/pages/SalaryCapPage.tsx
@frontend/src/pages/DashboardPage.tsx
@frontend/src/components/data-table/DataTable.tsx
@frontend/src/api/queries/teams.ts
@frontend/src/api/queries/cap.ts
@frontend/src/api/types.ts
@frontend/src/lib/format.ts

**Tech stack available:** TanStack Query, shadcn/ui (Chart, Card, Progress, Badge, Alert, Table, Select), Recharts, DataTable
**Established patterns:** Query hooks, formatSalary, DataTable with column defs, team selector via Select
**Constraining decisions:**
- Cap data from GET /api/cap/teams/{teamId} (TeamCapSummarySchema)
- Allotments from GET /api/cap/teams/{teamId}/allotments (AllotmentsSchema)
- Snapshot from GET /api/teams/{teamId}/snapshot (TeamSnapshotSchema) for dashboard
- Use shadcn Chart (Recharts) with ChartConfig for theming — use var(--color-{key}) not hardcoded colors
- Salaries in millions — formatSalary for display
</context>

<tasks>

<task type="auto">
  <name>Task 1: Salary cap page with chart, penalty table, and allotments</name>
  <files>frontend/src/pages/SalaryCapPage.tsx, frontend/src/components/cap/CapChart.tsx, frontend/src/components/cap/CapSummaryCards.tsx, frontend/src/components/cap/AllotmentsCard.tsx</files>
  <action>
1. Create `frontend/src/components/cap/CapSummaryCards.tsx`:
   - Display key cap figures as stat cards: Total Salary, Total Penalty Exposure, Roster Count
   - Each card: large formatted number + label
   - Show salary_by_type breakdown (NG, SD, FG amounts)

2. Create `frontend/src/components/cap/CapChart.tsx`:
   - Stacked bar chart using shadcn ChartContainer + Recharts BarChart
   - Show salary breakdown by contract type (NG, SD, FG) from salary_by_type dict
   - Use ChartConfig with CSS variables: var(--color-ng), var(--color-sd), var(--color-fg)
   - Include ChartTooltip with ChartTooltipContent for hover details
   - Keep it simple — one stacked bar showing composition, not complex multi-series

3. Create `frontend/src/components/cap/AllotmentsCard.tsx`:
   - Card showing remaining allotments from AllotmentsSchema
   - Display: Franchise Tags (X remaining), Buyout/Restructure (X remaining), Tenders (X remaining), July 1 Tenders (X remaining)
   - Use Progress bars or simple "X of Y" display

4. Rewrite `frontend/src/pages/SalaryCapPage.tsx`:
   - Team selector at top (same pattern as roster page — reuse or share via nuqs)
   - Fetch cap data with useTeamCap(teamId) and allotments with useTeamAllotments(teamId)
   - Layout: CapSummaryCards row → CapChart → Player penalties DataTable → AllotmentsCard
   - Player penalties table using DataTable with columns: Player Name (link to detail), Position, Salary (formatted), Contract Type, Years Remaining, Total Penalty (formatted)
   - Sort by salary descending by default
   - Loading skeletons while data fetches
  </action>
  <verify>cd frontend && pnpm build succeeds; cap page shows chart, summary, penalties table, and allotments</verify>
  <done>Salary cap page renders team cap summary with bar chart, sortable penalty table, and allotments display for any selected team</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard page with action items, cap overview, and roster summary</name>
  <files>frontend/src/pages/DashboardPage.tsx, frontend/src/components/dashboard/ActionItems.tsx, frontend/src/components/dashboard/CapOverview.tsx, frontend/src/components/dashboard/RosterSummary.tsx</files>
  <action>
1. Create `frontend/src/components/dashboard/ActionItems.tsx`:
   - Accepts roster (RosterEntrySchema[]) and allotments (AllotmentsSchema)
   - Surface actionable items based on data:
     * Players with 1 year remaining (expiring contracts — decision needed)
     * Available allotments (franchise tags, tenders, buyouts still available)
   - Each item: player name (linked to detail page), action type, brief description
   - If no action items: "No pending actions" message
   - Sort by urgency/importance

2. Create `frontend/src/components/dashboard/CapOverview.tsx`:
   - Accepts TeamCapSummarySchema
   - Compact cap summary: Total Salary as large number, salary by type as small breakdown
   - Progress bar showing cap usage percentage (if cap ceiling available, otherwise just total)
   - Link to full salary cap page

3. Create `frontend/src/components/dashboard/RosterSummary.tsx`:
   - Accepts roster (RosterEntrySchema[])
   - Show: Total roster count, count by position group, roster status breakdown
   - Link to roster page

4. Rewrite `frontend/src/pages/DashboardPage.tsx`:
   - Team selector at top (same pattern, share via nuqs or consistent component)
   - Fetch snapshot with useTeamSnapshot(teamId) — gets roster + cap + allotments in one call
   - Layout: page heading → ActionItems (prominent, top) → grid with CapOverview and RosterSummary
   - Loading skeletons while snapshot fetches
   - Action-item driven: the first thing GMs see is what needs attention
  </action>
  <verify>cd frontend && pnpm build succeeds; dashboard shows action items, cap overview, and roster summary</verify>
  <done>Dashboard surfaces expiring contracts and available actions prominently; cap overview and roster summary provide at-a-glance team status</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Salary cap page with chart and penalty table, and action-driven dashboard</what-built>
  <how-to-verify>
    1. Run: cd frontend && pnpm dev (backend must be running)
    2. Visit: http://localhost:5173/ (Dashboard)
    3. Test: Select a team — dashboard populates with action items, cap overview, roster summary
    4. Confirm: Expiring contracts (1 year remaining) appear in action items
    5. Confirm: Available allotments shown
    6. Confirm: Cap overview shows formatted salary totals
    7. Visit: http://localhost:5173/cap (Salary Cap)
    8. Test: Select a team — cap chart, summary cards, penalty table, allotments render
    9. Confirm: Bar chart shows salary breakdown by contract type
    10. Confirm: Penalty table is sortable with formatted salary values
    11. Test: Click a player in penalty table — navigates to player detail
    12. Test: Navigate between all pages (Dashboard → Roster → Player → Cap) — smooth flow
    13. Confirm: Team selector is consistent across pages
    14. Confirm: All data feels trustworthy — numbers formatted, consistent, match expectations

    Phase 8 complete.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && pnpm build` succeeds
- [ ] Salary cap page shows chart, summary, penalties, allotments
- [ ] Dashboard surfaces action items prominently
- [ ] Team selector consistent across all pages
- [ ] All navigation paths work (dashboard ↔ roster ↔ player detail ↔ cap)
- [ ] All salary values formatted as USD
- [ ] Phase 8 complete — all 4 plans done
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Dashboard is action-item driven — GMs see what needs attention first
- Salary cap page provides clear cap picture with visualization
- Full app flow works end-to-end across all 4 pages
- Phase 8 complete
  </success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-ui/08-04-SUMMARY.md`
</output>
