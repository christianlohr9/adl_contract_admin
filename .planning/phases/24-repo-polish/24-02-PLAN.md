---
phase: 24-repo-polish
plan: 02
type: execute
---

<objective>
Clean up repo hygiene — fix .gitignore to exclude dev artifacts, and add the ADL shield logo as the favicon.

Purpose: The repo should look professional and portfolio-ready. No database files, spreadsheets, or dev artifacts visible. The app gets its own branding via favicon.
Output: Clean .gitignore, favicon added, repo root free of artifacts.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-repo-polish/24-CONTEXT.md
@.planning/phases/24-repo-polish/24-01-SUMMARY.md

**Repo hygiene findings:**
Root directory has untracked artifacts that should be gitignored:
- `adl_contract_admin.db` (empty, 0 bytes)
- `src/adl_contract_admin.db` (empty, 0 bytes)
- `2026 ADL Contract Admin.xlsx` (512KB spreadsheet)
- `adl_logo.png` (36KB — ADL shield logo, needed for favicon conversion)
- `Analytics Dynasty League Bylaws 2025.md` (reference document)

Current .gitignore: Standard Python template but missing entries for:
- `*.db` (SQLite database files)
- `*.xlsx` (spreadsheets)
- `*.png` in root (logo source file)
- `*.md` reference docs in root that aren't README

Frontend has NO favicon currently — index.html has no `<link rel="icon">` tag, no `public/` directory.

The `adl_logo.png` file in the repo root is the ADL shield logo the user wants as favicon.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix .gitignore and clean repo artifacts</name>
  <files>.gitignore</files>
  <action>
Add these entries to the root .gitignore (at the end, under a clear section header):

```
# Project artifacts
*.db
*.xlsx
```

Do NOT gitignore .png or .md broadly — the logo will be moved into the project as a favicon, and the bylaws doc is a reference the user keeps locally.

Do NOT `git rm` anything — these files are already untracked. The .gitignore just prevents accidental `git add`.

Verify the untracked files won't show in future `git status` after the .gitignore update.
  </action>
  <verify>
    git status — .db and .xlsx files no longer appear as untracked.
  </verify>
  <done>.gitignore updated. SQLite and spreadsheet files excluded. Untracked artifacts hidden from git status.</done>
</task>

<task type="auto">
  <name>Task 2: Add ADL shield favicon</name>
  <files>
    frontend/public/favicon.ico,
    frontend/public/favicon.svg,
    frontend/index.html
  </files>
  <action>
1. Create `frontend/public/` directory if it doesn't exist.

2. Convert `adl_logo.png` from the repo root to favicon formats:
   - Use ImageMagick (`convert`) or Python Pillow to create `favicon.ico` (multi-size: 16x16, 32x32, 48x48) from the PNG.
   - If ImageMagick is available: `convert adl_logo.png -resize 32x32 frontend/public/favicon.ico`
   - If not available, copy the PNG as-is and reference it directly — browsers handle PNG favicons fine.
   - Also copy as `frontend/public/favicon.png` for modern browsers.

3. Update `frontend/index.html` to add favicon link in `<head>`:
   ```html
   <link rel="icon" type="image/png" href="/favicon.png" />
   ```

4. Verify the favicon is served correctly by checking the built output or dev server.

Do NOT remove `adl_logo.png` from root — it's untracked and the user's source file.
  </action>
  <verify>
    ls frontend/public/favicon* — favicon file exists.
    grep 'favicon' frontend/index.html — link tag present.
    cd frontend && npx tsc --noEmit — no type errors.
  </verify>
  <done>ADL shield favicon added to frontend/public/. index.html updated with favicon link. Default React logo replaced with ADL branding.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] .gitignore excludes *.db and *.xlsx
- [ ] `git status` no longer shows .db/.xlsx as untracked
- [ ] Favicon file exists in frontend/public/
- [ ] index.html has favicon `<link>` tag
- [ ] Phase 24 complete — all cleanup done
</verification>

<success_criteria>

- .gitignore properly excludes dev artifacts
- ADL shield favicon added and referenced
- Repo looks professional and portfolio-ready
- Phase 24: Repo Polish complete
</success_criteria>

<output>
After completion, create `.planning/phases/24-repo-polish/24-02-SUMMARY.md`
</output>
