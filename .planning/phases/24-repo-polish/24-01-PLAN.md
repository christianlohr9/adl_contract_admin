---
phase: 24-repo-polish
plan: 01
type: execute
---

<objective>
Strip AI-generated comment slop, remove dead code, and purge unused dependencies across the entire codebase.

Purpose: Every line in every file should earn its place. Remove separator comments, redundant docstrings, unused imports, and dependencies nothing references.
Output: Lean, professional codebase with no comment noise or dead weight.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-repo-polish/24-CONTEXT.md

**Scope from CONTEXT.md:**
- No refactoring — don't restructure or rewrite working code
- No new features — purely subtractive (except favicon in plan 02)
- .planning/ directory stays

**Comment slop findings (from codebase scan):**
~16 Python service files and 1 TypeScript file have section separator comments like:
```
# ---------------------------------------------------------------------------
# Section name
# ---------------------------------------------------------------------------
```
Plus redundant docstrings that just restate function names.

Worst offenders by separator count:
- src/app/services/buyouts.py (~12 separators)
- src/app/services/eligibility.py (~9 separators)
- src/app/services/roster_eligibility.py
- src/app/services/cap_summary.py
- src/app/services/tenders.py
- src/app/services/extensions.py
- src/app/services/franchise_tags.py
- src/app/services/window_status.py
- src/app/services/cap_penalties.py
- src/app/services/epv.py
- src/app/services/allotments.py
- src/app/services/contract_classifier.py
- src/app/services/rules.py
- src/app/services/historical_sync.py
- src/app/services/player_season_sync.py
- src/app/core/db.py (1 redundant docstring)
- frontend/src/api/types.ts (separator comments)

**Dead code findings:**
- src/app/api/teams.py line 11: unused `from app.models.player import Player`
- openpyxl in pyproject.toml dependencies — only used by scripts/validate_*.py, not by the app itself
- zustand in frontend/package.json — zero imports in frontend/src/
- pytest-httpx in pyproject.toml dev deps — zero imports in tests/

**What to keep:**
- Meaningful comments that explain WHY (business logic, non-obvious decisions)
- Section headers that are genuinely helpful WITHOUT the separator lines (e.g., keep "# RFA tender calculations" but remove the dashes above/below)
- All actual code — this is subtractive only
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip comment slop from Python and TypeScript files</name>
  <files>
    src/app/services/buyouts.py,
    src/app/services/eligibility.py,
    src/app/services/roster_eligibility.py,
    src/app/services/cap_summary.py,
    src/app/services/tenders.py,
    src/app/services/extensions.py,
    src/app/services/franchise_tags.py,
    src/app/services/window_status.py,
    src/app/services/cap_penalties.py,
    src/app/services/epv.py,
    src/app/services/allotments.py,
    src/app/services/contract_classifier.py,
    src/app/services/rules.py,
    src/app/services/historical_sync.py,
    src/app/services/player_season_sync.py,
    src/app/core/db.py,
    frontend/src/api/types.ts
  </files>
  <action>
For each file listed above:

1. Remove ALL separator comment lines (lines that are only `# ---...---` or `// ---...---`). These add visual noise without information.
2. Keep section header comments IF they provide genuine organizational value (e.g., "# RFA tender calculations"), but remove the separator lines above/below them. A bare comment like `# Helpers` on its own line is fine.
3. Remove redundant docstrings that just restate the function name or return type (e.g., `"""Yield an async database session."""` on `get_session()`). If the function name already says what it does, the docstring adds nothing.
4. Keep docstrings that explain WHY, document business rules, or clarify non-obvious behavior.
5. Also scan any other Python/TS files not in the list above — if they have the same separator pattern, clean them too.

Do NOT remove inline comments that explain business logic or non-obvious decisions.
Do NOT modify any actual code — only comments and docstrings.
  </action>
  <verify>
    ruff check src/ — no new lint errors introduced.
    cd frontend && npx tsc --noEmit — no type errors.
  </verify>
  <done>All separator comment lines removed. Redundant docstrings removed. Meaningful comments preserved. No code changes, only comment/docstring removal.</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused imports and dependencies</name>
  <files>
    src/app/api/teams.py,
    pyproject.toml,
    frontend/package.json
  </files>
  <action>
1. **Unused import:** Remove `from app.models.player import Player` from src/app/api/teams.py (line 11) — confirmed unused.

2. **Unused Python dep:** Remove `openpyxl` from pyproject.toml [project].dependencies. It's only used by scripts/validate_*.py (one-off validation scripts that ran during v1.3), not by the app. The scripts themselves stay — they just won't be runnable without manually installing openpyxl, which is fine since they're historical artifacts.

3. **Unused Python dev dep:** Remove `pytest-httpx` from pyproject.toml [dependency-groups].dev — zero imports in tests/.

4. **Unused frontend dep:** Remove `zustand` from frontend/package.json dependencies — zero imports in frontend/src/.

5. Run `uv lock` after pyproject.toml changes to regenerate lockfile.

6. Run `cd frontend && pnpm install` (or npm install) after package.json changes to update lockfile.

Do NOT remove ruff or mypy from dev deps — they're CLI tools used via configuration.
Do NOT remove scripts/ directory — those are historical validation artifacts worth keeping.
  </action>
  <verify>
    uv lock — succeeds without errors.
    cd frontend && pnpm install — succeeds without errors.
    ruff check src/ — passes.
    cd frontend && npx tsc --noEmit — passes.
  </verify>
  <done>Unused import removed from teams.py. openpyxl and pytest-httpx removed from pyproject.toml. zustand removed from frontend/package.json. Lockfiles regenerated. All checks pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `ruff check src/` passes
- [ ] `cd frontend && npx tsc --noEmit` passes
- [ ] No separator comment lines remain in listed files
- [ ] No unused deps in pyproject.toml or package.json
- [ ] Lockfiles regenerated
</verification>

<success_criteria>

- All separator comments stripped from ~16 Python files and 1 TS file
- Redundant docstrings removed
- Meaningful comments preserved
- Unused import removed from teams.py
- openpyxl, pytest-httpx removed from Python deps
- zustand removed from frontend deps
- All linting and type checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/24-repo-polish/24-01-SUMMARY.md`
</output>
