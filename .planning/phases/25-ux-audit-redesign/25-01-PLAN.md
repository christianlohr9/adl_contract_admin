---
phase: 25-ux-audit-redesign
plan: 01
type: execute
---

<objective>
Establish the dark-mode-only Sleeper-inspired base theme and per-team accent color system via CSS custom properties.

Purpose: Foundation for entire redesign — every subsequent plan depends on the dark theme and team color system being in place.
Output: Dark base theme with 32 team accent color overrides, team selection hook with localStorage persistence.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-ux-audit-redesign/25-RESEARCH.md
@.planning/phases/25-ux-audit-redesign/25-CONTEXT.md

# Key files:
@frontend/src/index.css
@frontend/src/main.tsx
@frontend/src/App.tsx

**Tech stack available:** Tailwind v4 + shadcn/ui (OKLCH theming), React Router, nuqs, localStorage
**Established patterns:** shadcn CSS variable theming, `@custom-variant dark`, OKLCH color space

**Constraining decisions:**
- [RESEARCH]: Override only 3-4 CSS variables per team (--primary, --accent, --sidebar-primary, --ring) to keep CSS manageable (~160 lines for 32 teams)
- [RESEARCH]: Dark base stays constant, team colors are accent-only
- [RESEARCH]: Use OKLCH lightness >= 0.45 for all team primary colors on dark backgrounds
- [CONTEXT]: Dark mode only — no light mode toggle
- [CONTEXT]: Sleeper-inspired aesthetic — deep navy/black backgrounds, layered card surfaces, bold typography
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark base theme + 32 team accent color overrides</name>
  <files>frontend/src/index.css</files>
  <action>
  1. Remove the `:root` (light mode) block entirely — app is dark-only.
  2. Update `.dark` block with Sleeper-inspired values:
     - --background: oklch(0.10 0 0) (deeper than default)
     - --card: oklch(0.16 0 0) (layered surface)
     - --popover: oklch(0.16 0 0)
     - --muted: oklch(0.20 0 0)
     - --border: oklch(1 0 0 / 8%)
     - --input: oklch(1 0 0 / 12%)
     - --sidebar: oklch(0.12 0 0) (slightly lighter than bg)
     - Keep foreground/text values as-is (near-white)
  3. Add `[data-team="XXX"]` selectors for all 32 NFL teams AFTER the `.dark` block. Each team overrides only: --primary, --primary-foreground (if needed for contrast), --ring, --sidebar-primary. Use the team color reference from RESEARCH.md. For dark-background problem teams (BAL, CHI, HOU, IND, NE, NYG, NYJ, PHI, SEA, TEN, WAS, GB, BUF, DAL, LAR, CLE), boost OKLCH lightness to >= 0.45 or use their secondary color.
  4. Set --primary-foreground appropriately per team (white for most, dark for bright teams like PIT gold, NO gold, CLE orange).
  5. Add `html { color-scheme: dark; }` to force browser dark mode chrome.

  Reference NFL team colors from RESEARCH.md <nfl_team_colors> section. Convert hex to OKLCH using the perceptual uniformity principle — adjust lightness for dark-bg readability.

  Do NOT add light mode fallback. Do NOT override more than 4 variables per team.
  </action>
  <verify>
  - `grep -c 'data-team' frontend/src/index.css` returns 32 (one per team)
  - No `:root` block with light theme values remains
  - `.dark` block exists with updated background values
  - `html { color-scheme: dark; }` present
  </verify>
  <done>Dark-only base theme with Sleeper-inspired values. All 32 team accent overrides defined. No light mode.</done>
</task>

<task type="auto">
  <name>Task 2: Team selection hook with localStorage persistence</name>
  <files>frontend/src/hooks/useTeamSelection.ts, frontend/src/main.tsx</files>
  <action>
  1. Create `frontend/src/hooks/useTeamSelection.ts`:
     - `useTeamSelection()` hook that returns `{ selectedTeam, selectTeam, clearTeam }`.
     - `selectTeam(teamId: number, teamAbbr: string)`: sets `data-team` attribute on `document.documentElement`, stores `{ id, abbr }` in localStorage key `adl-selected-team`.
     - `clearTeam()`: removes `data-team` attribute, clears localStorage.
     - On hook mount, restore from localStorage if present (set `data-team` attribute).
     - Export a standalone `restoreTeamTheme()` function (non-hook) that reads localStorage and applies `data-team` — for use in main.tsx before React mounts.

  2. Update `frontend/src/main.tsx`:
     - Import and call `restoreTeamTheme()` before `createRoot()` to prevent flash of unthemed content.
     - Ensure `document.documentElement.classList.add('dark')` is called (force dark mode).

  The hook should work with the existing nuqs `team` query param used by DashboardPage and ContractManagementPage. It does NOT replace the URL team param — it adds the visual theming layer on top.
  </action>
  <verify>
  - `frontend/src/hooks/useTeamSelection.ts` exists with exported hook and restoreTeamTheme
  - `frontend/src/main.tsx` calls restoreTeamTheme() and adds dark class
  - TypeScript compiles: `cd frontend && npx tsc --noEmit`
  </verify>
  <done>useTeamSelection hook persists team choice to localStorage and applies data-team attribute. Theme restored on page load before React mounts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npx tsc --noEmit` passes
- [ ] `cd frontend && npm run build` succeeds
- [ ] Dark theme variables in index.css — no light mode block
- [ ] 32 team color overrides present in index.css
- [ ] useTeamSelection hook created and exports correctly
- [ ] restoreTeamTheme called in main.tsx
</verification>

<success_criteria>
- Dark-only Sleeper-inspired base theme active
- All 32 team accent colors defined as CSS variable overrides
- Team selection hook persists to localStorage and applies data-team attribute
- No TypeScript errors, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/25-ux-audit-redesign/25-01-SUMMARY.md`
</output>
