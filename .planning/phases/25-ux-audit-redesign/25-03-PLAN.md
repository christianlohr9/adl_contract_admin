---
phase: 25-ux-audit-redesign
plan: 03
type: execute
---

<objective>
Redesign the dashboard to be action-oriented with prominent deadline countdowns, and apply Sleeper-inspired typography and spacing across the app.

Purpose: The dashboard is the first thing GMs see after selecting their team — it must feel like a real sports product with clear urgency signals.
Output: Redesigned dashboard with bold countdowns, polished cap/action cards, consistent Sleeper-inspired typography.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-ux-audit-redesign/25-RESEARCH.md
@.planning/phases/25-ux-audit-redesign/25-CONTEXT.md
@.planning/phases/25-ux-audit-redesign/25-01-SUMMARY.md
@.planning/phases/25-ux-audit-redesign/25-02-SUMMARY.md

# Key files:
@frontend/src/pages/DashboardPage.tsx
@frontend/src/components/dashboard/ActionItems.tsx
@frontend/src/components/dashboard/CapOverview.tsx
@frontend/src/components/dashboard/RosterSummary.tsx
@frontend/src/components/contracts/DeadlineCountdown.tsx
@frontend/src/components/contracts/useDeadlineCards.ts
@frontend/src/index.css

**Established patterns:** DeadlineCountdown component exists on /contracts page, team snapshot API, shadcn Card/Badge
**Constraining decisions:**
- [CONTEXT]: Prominent deadline countdown — big, bold, can't-miss-it urgency
- [RESEARCH]: Primary number BIG and bold (3xl-5xl), detail text small and muted
- [RESEARCH]: Cards with rounded-xl, left-colored borders, generous padding
- [RESEARCH]: Semi-transparent borders, layered card surfaces
- [CONTEXT]: No new features — pure visual overhaul of existing functionality
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign dashboard layout with prominent deadline and action cards</name>
  <files>frontend/src/pages/DashboardPage.tsx, frontend/src/components/dashboard/ActionItems.tsx, frontend/src/components/dashboard/CapOverview.tsx, frontend/src/components/dashboard/RosterSummary.tsx</files>
  <action>
  1. Redesign DashboardPage layout structure:
     - Remove the team dropdown selector — team is now selected via splash screen. Get team from useTeamSelection hook or from URL query param.
     - Top section: Full-width deadline countdown card. Reuse data from the existing deadline system (useDeadlineCards hook or fetch from calendar API). Show the most urgent deadline prominently: large countdown numbers (text-5xl font-bold text-primary), action name, urgency color.
     - Middle section: Grid of action summary cards — cap overview, pending actions count, roster summary. Use border-l-4 with team accent (border-l-primary).
     - Bottom section: Compact roster quick-view or recent activity.

  2. Redesign CapOverview component:
     - Large cap number (text-4xl font-bold) with "remaining" label in muted text.
     - Total cap and used cap as secondary stats below.
     - Use Card with Sleeper-inspired styling: bg-card, rounded-xl, generous padding (p-6).

  3. Redesign ActionItems component:
     - Show counts of eligible players per action type as bold badges.
     - Each action type as a compact card with count + action name.
     - Clickable — navigate to /contracts page filtered to that action.

  4. Redesign RosterSummary component:
     - Key stat: total roster size as large number.
     - Secondary: position breakdown as small muted text.
     - Keep compact — this is tertiary content.

  5. Apply Sleeper typography throughout the page:
     - Page title: text-3xl font-bold
     - Stat numbers: text-4xl or text-5xl font-bold with tabular-nums
     - Labels: text-xs uppercase tracking-wider text-muted-foreground
     - Remove the "GM action center" subtitle — let the design speak.

  Do NOT add new data fetching — use existing hooks and API data. Do NOT add animations or transitions beyond hover states.
  </action>
  <verify>
  - DashboardPage no longer has team dropdown
  - Deadline countdown appears at top of dashboard
  - Cap number displayed large and bold
  - TypeScript compiles: `cd frontend && npx tsc --noEmit`
  - Build succeeds: `cd frontend && npm run build`
  </verify>
  <done>Dashboard is action-oriented with prominent deadline, bold cap/stat numbers, Sleeper-inspired card styling.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Sleeper-inspired global styling and typography</name>
  <files>frontend/src/index.css, frontend/src/components/layout/AppLayout.tsx, frontend/src/pages/ContractManagementPage.tsx, frontend/src/pages/RosterPage.tsx, frontend/src/pages/SalaryCapPage.tsx, frontend/src/pages/CalendarPage.tsx</files>
  <action>
  1. Update global styles in index.css:
     - Add `font-variant-numeric: tabular-nums;` to body/html for aligned number columns.
     - Ensure the Geist font is applied consistently.
     - Add utility classes if needed: `.stat-number { @apply text-4xl font-bold tabular-nums; }`, `.stat-label { @apply text-xs uppercase tracking-wider text-muted-foreground; }`

  2. Update AppLayout:
     - Ensure main content area has consistent padding and max-width.
     - Background should be --background (the deep dark from Plan 01).

  3. Remove team dropdown selectors from ContractManagementPage and RosterPage:
     - These pages currently have their own team dropdowns. Replace with team from useTeamSelection hook (read from localStorage/context) or keep the URL query param but auto-populate from the selected team.
     - If a page is loaded without a team query param, use the selected team from useTeamSelection.

  4. Quick typography pass on remaining pages:
     - ContractManagementPage: bold page title, consistent card styling
     - RosterPage: bold page title, clean table styling
     - SalaryCapPage: large cap numbers, consistent card styling
     - CalendarPage: consistent card styling
     - Do NOT restructure these pages — just update typography and card styling to match the Sleeper aesthetic.

  Keep changes minimal on non-dashboard pages — this task is about consistency, not redesigning every page.
  </action>
  <verify>
  - Team dropdowns removed from ContractManagementPage and RosterPage (or auto-populated)
  - tabular-nums applied globally
  - All pages render without errors
  - Build succeeds: `cd frontend && npm run build`
  </verify>
  <done>Consistent Sleeper-inspired typography and styling across all pages. Team selection flows from splash screen through all pages.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run build` succeeds
- [ ] Dashboard shows deadline countdown prominently
- [ ] Cap and action stats use large bold numbers
- [ ] No team dropdown on any page (team comes from splash selection)
- [ ] All pages have consistent typography and card styling
</verification>

<success_criteria>
- Dashboard is action-oriented with prominent deadline countdown
- All stat numbers are large, bold, tabular-nums
- Typography is consistent Sleeper-style across all pages
- Team selection persists from splash through all pages
- No TypeScript errors, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/25-ux-audit-redesign/25-03-SUMMARY.md`
</output>
