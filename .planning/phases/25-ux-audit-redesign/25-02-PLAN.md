---
phase: 25-ux-audit-redesign
plan: 02
type: execute
---

<objective>
Create the splash screen team picker as the app entry point and add team branding to the sidebar.

Purpose: The splash screen sets the "real product" tone immediately. Sidebar branding makes each GM feel like the app is theirs.
Output: Full-screen team picker at `/`, team-branded sidebar, updated routing.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-ux-audit-redesign/25-RESEARCH.md
@.planning/phases/25-ux-audit-redesign/25-CONTEXT.md
@.planning/phases/25-ux-audit-redesign/25-01-SUMMARY.md

# Key files:
@frontend/src/App.tsx
@frontend/src/components/layout/AppLayout.tsx
@frontend/src/components/layout/AppSidebar.tsx
@frontend/src/hooks/useTeamSelection.ts
@frontend/src/api/queries/teams.ts

**Established patterns:** React Router nested routes, shadcn Sidebar, useTeamSelection hook from Plan 01
**Constraining decisions:**
- [CONTEXT]: Splash screen = all 32 teams on dark background, click yours → you're in
- [RESEARCH]: Keep splash dead simple — dark bg, centered title, grid of team buttons, no animations beyond hover
- [RESEARCH]: Store team selection in localStorage, auto-navigate on return visits
- [CONTEXT]: Team abbreviation badges with team colors as primary approach (logos nice-to-have)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SplashPage with team picker grid</name>
  <files>frontend/src/pages/SplashPage.tsx, frontend/src/lib/teams.ts</files>
  <action>
  1. Create `frontend/src/lib/teams.ts`:
     - Export a `TEAMS` array with all 32 ADL franchises: `{ id: number, name: string, abbr: string }`.
     - The team IDs must match the backend team IDs. Query the existing `useTeams()` hook pattern to understand the data shape. If IDs aren't known statically, the SplashPage should fetch teams from the API using the existing useTeams() hook.
     - Include the NFL team abbreviation for each franchise (for data-team CSS matching).

  2. Create `frontend/src/pages/SplashPage.tsx`:
     - Full-screen dark background (bg-[oklch(0.06_0_0)] — darker than app bg for contrast).
     - Centered layout: ADL logo/shield at top (use the favicon PNG from `frontend/public/favicon.png` as a larger display), app title "ADL Contract Admin" in bold white, subtitle "Select your franchise" in muted text.
     - Grid of 32 team buttons: 4 cols on mobile, 8 cols on desktop. Each button shows the team abbreviation in a rounded badge using the team's accent color (apply a temporary `data-team` attribute on hover to preview the color, or use inline styles from a color map).
     - On click: call `selectTeam(team.id, team.abbr)` from useTeamSelection hook, then navigate to `/dashboard?team={team.id}`.
     - On mount: check localStorage for saved team. If found, auto-redirect to `/dashboard?team={savedId}`.
     - Include a subtle "ADL" or shield watermark/branding element.

  Keep the component under 100 lines. No complex animations — just hover opacity/scale transitions.
  </action>
  <verify>
  - `frontend/src/pages/SplashPage.tsx` exists
  - `frontend/src/lib/teams.ts` exists (or team data is fetched from API)
  - Component imports useTeamSelection hook
  - TypeScript compiles: `cd frontend && npx tsc --noEmit`
  </verify>
  <done>SplashPage renders 32-team grid on dark background. Click selects team and navigates. Return visitors auto-redirect.</done>
</task>

<task type="auto">
  <name>Task 2: Update routing + sidebar team branding</name>
  <files>frontend/src/App.tsx, frontend/src/components/layout/AppSidebar.tsx, frontend/src/components/layout/AppLayout.tsx</files>
  <action>
  1. Update `frontend/src/App.tsx` routing:
     - Add SplashPage as the index route OUTSIDE the AppLayout wrapper.
     - Move all current routes under a path prefix or keep them as-is but with AppLayout wrapping.
     - Structure:
       ```
       <Routes>
         <Route index element={<SplashPage />} />
         <Route element={<AppLayout />}>
           <Route path="dashboard" element={<DashboardPage />} />
           <Route path="roster" element={<RosterPage />} />
           <Route path="roster/:playerId" element={<PlayerDetailPage />} />
           <Route path="cap" element={<SalaryCapPage />} />
           <Route path="contracts" element={<ContractManagementPage />} />
           <Route path="calendar" element={<CalendarPage />} />
         </Route>
       </Routes>
       ```
     - Note: Dashboard moves from `/` to `/dashboard`. Update any hardcoded `/` links.

  2. Update `frontend/src/components/layout/AppSidebar.tsx`:
     - At the top of the sidebar header, show the selected team's abbreviation/name with team accent color styling.
     - Use the useTeamSelection hook to get the current team.
     - Add a "Switch Team" button/link that navigates back to `/` (splash screen) and calls clearTeam().
     - Update the Dashboard nav item URL from `/` to `/dashboard`.
     - Style the active nav item with team accent color (it should already work via --sidebar-primary CSS variable).

  3. Update `frontend/src/components/layout/AppLayout.tsx`:
     - On mount, if no team is selected (no data-team attribute / no localStorage), redirect to `/` (splash).
     - This prevents accessing app pages without a team selected.
  </action>
  <verify>
  - Dashboard route is now `/dashboard` not `/`
  - `/` renders SplashPage (no sidebar)
  - AppSidebar shows team name/abbr at top
  - "Switch Team" button exists in sidebar
  - Build succeeds: `cd frontend && npm run build`
  </verify>
  <done>Splash at /, app routes under AppLayout, sidebar shows team branding with switch option.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Splash screen team picker and team-branded sidebar navigation</what-built>
  <how-to-verify>
    1. Run: `cd frontend && npm run dev`
    2. Visit: http://localhost:5173
    3. Confirm: Dark splash screen with "ADL Contract Admin" title and 32 team badges
    4. Click any team badge — should navigate to /dashboard with team selected
    5. Check sidebar: team name/abbreviation visible at top, accent colors applied
    6. Click "Switch Team" in sidebar — should return to splash screen
    7. Close browser tab, reopen http://localhost:5173 — should auto-redirect to last team's dashboard
    8. Check: team accent color visible in sidebar active state, badges, any primary-colored elements
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run build` succeeds
- [ ] Splash screen renders at /
- [ ] Team selection persists across page reloads
- [ ] Sidebar shows team branding
- [ ] All existing routes still work under new structure
</verification>

<success_criteria>
- Splash screen is the app entry point with 32 team picker
- Team selection applies accent colors app-wide via CSS variables
- Sidebar shows team identity with switch option
- Return visitors auto-navigate to their team's dashboard
- No broken routes or TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-ux-audit-redesign/25-02-SUMMARY.md`
</output>
