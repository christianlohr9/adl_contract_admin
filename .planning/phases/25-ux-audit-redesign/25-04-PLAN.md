---
phase: 25-ux-audit-redesign
plan: 04
type: execute
---

<objective>
Polish contract tool result layouts and player detail page to complete the Sleeper-inspired redesign.

Purpose: Contract tools are the core value — they must present calculation results clearly with the final number prominent and details expandable.
Output: Card-based layouts for small result sets, polished tables for large sets, refined player detail page. Phase 25 complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-ux-audit-redesign/25-RESEARCH.md
@.planning/phases/25-ux-audit-redesign/25-CONTEXT.md
@.planning/phases/25-ux-audit-redesign/25-01-SUMMARY.md
@.planning/phases/25-ux-audit-redesign/25-02-SUMMARY.md
@.planning/phases/25-ux-audit-redesign/25-03-SUMMARY.md

# Key files:
@frontend/src/pages/PlayerDetailPage.tsx
@frontend/src/components/player/PlayerHeader.tsx
@frontend/src/components/player/tools/ExtensionsTab.tsx
@frontend/src/components/player/tools/TagsTab.tsx
@frontend/src/components/player/tools/TendersTab.tsx
@frontend/src/components/player/tools/BuyoutTab.tsx
@frontend/src/components/player/tools/FifthYearTab.tsx
@frontend/src/components/player/tools/PPETab.tsx
@frontend/src/components/player/tools/IneligibleAlert.tsx

**Established patterns:** Tool tab components render calculation results, IneligibleAlert for gated tools
**Constraining decisions:**
- [CONTEXT]: Cards for small result sets (tenders, tags, buyout, 5YO, PPE), polished tables for large sets (extensions)
- [CONTEXT]: Result + summary — final number prominent, key inputs summarized, detailed breakdown on expand
- [RESEARCH]: Card with border-l-4 border-l-primary, large result number (text-3xl font-bold), collapsible detail sections
- [RESEARCH]: shadcn Collapsible component for expand/collapse detail breakdowns
</context>

<tasks>

<task type="auto">
  <name>Task 1: Card-based layouts for small result sets + extension table polish</name>
  <files>frontend/src/components/player/tools/TagsTab.tsx, frontend/src/components/player/tools/TendersTab.tsx, frontend/src/components/player/tools/BuyoutTab.tsx, frontend/src/components/player/tools/FifthYearTab.tsx, frontend/src/components/player/tools/PPETab.tsx, frontend/src/components/player/tools/ExtensionsTab.tsx, frontend/src/components/player/tools/IneligibleAlert.tsx</files>
  <action>
  1. Redesign small-result tool tabs (Tags, Tenders, Buyout, 5YO, PPE) with card layout:
     - Each result as a Card with border-l-4 border-l-primary.
     - Result amount/salary as text-3xl font-bold text-primary at top.
     - Type/label as text-sm text-muted-foreground below the number.
     - Key inputs summarized as a brief line (e.g., "Based on 3 accrued seasons, QB positional avg").
     - Detail breakdown in a collapsible section — use shadcn Collapsible or a simple disclosure pattern. Show calculation inputs and formula when expanded.
     - For multi-result tools (Tenders may have ERFA + RFA results), use a grid of cards.

  2. Polish ExtensionsTab for large result sets:
     - Keep as a table (DataTable) but improve styling:
       - Bold salary numbers with tabular-nums
       - Muted secondary columns
       - Row hover with subtle bg change
       - Clean column headers with Sleeper-style uppercase labels
     - If currently showing raw data, add summary row or header showing count of extension options.

  3. Polish IneligibleAlert:
     - Style consistently with the dark theme — use a subtle muted card instead of bright alert for ineligible states.
     - Show the reason text clearly.

  Read each tool tab component first to understand the current data shape before redesigning. Do NOT change the data fetching or API types — only the presentation layer.
  </action>
  <verify>
  - All 6 tool tab components updated with new styling
  - Card-based layout for Tags, Tenders, Buyout, 5YO, PPE
  - Extension table has polished styling
  - TypeScript compiles: `cd frontend && npx tsc --noEmit`
  </verify>
  <done>Small result tools use card layout with prominent numbers and expandable details. Extension table polished with consistent typography.</done>
</task>

<task type="auto">
  <name>Task 2: Player detail page and header redesign</name>
  <files>frontend/src/pages/PlayerDetailPage.tsx, frontend/src/components/player/PlayerHeader.tsx</files>
  <action>
  1. Redesign PlayerHeader:
     - Player name as text-3xl font-bold.
     - Position and team as badge/pill with team accent color (bg-primary/20 text-primary).
     - Key contract info (salary, years remaining, contract type) as stat cards below the name — large numbers with muted labels.
     - Use a horizontal layout: name/position on left, key stats on right.
     - Add subtle team accent: a top border or left border with team color on the header card.

  2. Redesign PlayerDetailPage:
     - Remove the outer Card wrapper from tool tabs — let the tabs breathe with more space.
     - Style TabsList with Sleeper-inspired look: rounded-xl background, clean active indicator using team accent.
     - Tool tab content area: clean padding, no unnecessary nesting.
     - Back button: style as a subtle ghost button, consistent placement.

  Read PlayerHeader.tsx first to understand the current data fields before redesigning.
  </action>
  <verify>
  - PlayerHeader shows player name large, position as accent badge, stats as cards
  - Tab styling updated with Sleeper-inspired look
  - TypeScript compiles: `cd frontend && npx tsc --noEmit`
  - Build succeeds: `cd frontend && npm run build`
  </verify>
  <done>Player detail page has polished header with team accent, clean tab navigation, and professional tool result presentation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete UX redesign — splash screen, dark theme, team branding, dashboard, contract tools, player detail</what-built>
  <how-to-verify>
    1. Run: `cd frontend && npm run dev` (and backend if needed for API data)
    2. Visit: http://localhost:5173
    3. Splash screen: Dark background, 32 team badges, click one
    4. Dashboard: Prominent deadline countdown, bold cap numbers, action summary cards, team accent colors visible
    5. Sidebar: Team name at top, accent color on active nav item, "Switch Team" option
    6. Navigate to /contracts: Deadline cards, eligibility table, consistent dark styling
    7. Click a player: Player header with accent badge, contract tool tabs
    8. Check Tags/Tenders tabs: Card-based layout with large result numbers
    9. Check Extensions tab: Polished table with bold salary numbers
    10. Switch to a different team (via sidebar): Accent colors change throughout
    11. Overall: Does it feel like a real sports product? Would commissioners trust it over the spreadsheet?
  </how-to-verify>
  <resume-signal>Type "approved" to continue to phase completion, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `cd frontend && npm run build` succeeds
- [ ] All 6 contract tool tabs render correctly with new styling
- [ ] Player detail page header uses team accent colors
- [ ] Full flow works: splash → team select → dashboard → roster → player → tools
- [ ] Multiple teams tested — accent colors change correctly
- [ ] No TypeScript errors or console warnings
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Contract tool results prominently display final numbers with expandable details
- Player detail page feels professional and team-branded
- Full UX flow is cohesive from splash to tools
- Phase 25 complete
</success_criteria>

<output>
After completion, create `.planning/phases/25-ux-audit-redesign/25-04-SUMMARY.md`
</output>
